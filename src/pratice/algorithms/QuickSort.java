package pratice.algorithms;

public class QuickSort {
    public static void main(String[] args) {
        int array[] = {0, 4, 2, 8, 6, 2, 1, 5, 12};
    }
    public static int quicksort(int item){
        item = array[item];
        if(array.length < 2) return item;
        else {
            int placeholder = array[0], below[], above[];
            for(int i=0, i<=placeholder, i++){
               if(array[i<= placeholder) below[i] = array[i]; 
               else above[i] = array[i];
            }
        }
    }
}
